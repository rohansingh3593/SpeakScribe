[pytest]
# pytest's importlib mode does not always prepend the project root to sys.path,
# notably with some pytest 9 Windows installations.  The application currently
# uses top-level modules, so make that import location explicit for every test.
pythonpath = .
testpaths = tests

